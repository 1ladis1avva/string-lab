# string-lab
Small collection of utilities for string manipulation and generation.

## Folders
- `tools/` — scripts for modifying strings.
- `generators/` — generate new strings.
- `examples/` — sample input/output.
